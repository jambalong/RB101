# greeting will return nil. Ruby's interpreter sees that greeting variable was initialized
# however because the if statement didn't execute, greeting was not assigned to any object
# so greeting will return nil, because greeting is initialized to nil
# Ruby will not raise an exception
